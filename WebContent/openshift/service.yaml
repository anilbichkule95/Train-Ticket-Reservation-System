apiVersion: v1
kind: Service
metadata:
  name: trainbook
spec:
  selector:
    app: trainbook

  ports:
    - name: http
      port: 9090
      targetPort: 9090
      protocol: TCP

  type: ClusterIP